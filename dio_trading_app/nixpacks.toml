# Dio Trading App - Nixpacks build config
# Used by Railway to detect Python version and install dependencies

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT"
