[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r dio_trading_app/requirements.txt"]

[start]
cmd = "uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT --app-dir dio_trading_app"
